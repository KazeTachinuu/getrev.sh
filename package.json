{
  "name": "getrev.sh",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "bun test",
    "check": "tsc --noEmit && wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.124.0"
  }
}
